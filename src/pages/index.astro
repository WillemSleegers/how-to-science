---
import Layout from "@/layouts/main.astro"
import { loadNav } from "@/lib/nav"
const nav = loadNav()
---

<Layout title="How to Science">
  <div class="m-2 rounded-xl shadow-sm overflow-hidden bg-background min-h-[calc(100vh-1rem)]">
  <div class="px-8 py-8 flex flex-col lg:flex-row gap-16">

    <div class="prose prose-neutral dark:prose-invert lg:w-1/3 shrink-0">
      <h1>How to Science</h1>

      <h2>About</h2>
      <p>
        The scientific literature is vast, inconsistent, and hard to navigate. It can be genuinely difficult to assess a body of research and figure out what is true, what is contested, and what the current best practices actually are.
      </p>
      <p>
        This website is my attempt to get to the bottom of various topics in methodology and statistics. For each topic, I try to work through the literature and distill what it says — or doesn't say — into clear, practical guidance. It also contains materials such as scales and other resources for use in research.
      </p>

      <h2>Work in progress</h2>
      <p>
        Some sections are unfinished. Pages I consider not yet polished carry a warning at the top. That said, this site will always be a work in progress: best practices evolve, and the content will evolve with them. There are likely mistakes too, ranging from errors of substance to simple typos — if you spot one, please contact me or open an issue on GitHub.
      </p>

      <h2>Author</h2>
      <p>
        <a href="https://www.willemsleegers.com">Willem Sleegers</a>. If you've contributed and are missing from this list, please contact me or add yourself via a Pull Request.
      </p>
    </div>

    <div class="flex-1 space-y-10 pt-2">
      {nav.map((group) => (
        <div>
          <h2 class="text-xs font-semibold uppercase tracking-wide text-muted-foreground mb-3">{group.label}</h2>
          <div class="grid gap-3 sm:grid-cols-2">
            {group.sections.map((section) => (
              <a
                href={`/${section.indexSlug}`}
                class="block rounded-lg px-4 py-3 transition-colors bg-sidebar text-sidebar-foreground"
              >
                <div class="font-medium text-sm mb-1.5">{section.title}</div>
                <div class="text-xs leading-relaxed text-sidebar-foreground/60">
                  {section.pages.slice(0, 4).map((p) => p.title).join(" · ")}
                  {section.pages.length > 4 && " · …"}
                </div>
              </a>
            ))}
          </div>
        </div>
      ))}
    </div>

  </div>
  </div>
</Layout>
